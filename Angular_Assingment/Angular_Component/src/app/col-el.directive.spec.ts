import { ColElDirective } from './col-el.directive';

describe('ColElDirective', () => {
  it('should create an instance', () => {
    const directive = new ColElDirective();
    expect(directive).toBeTruthy();
  });
});
